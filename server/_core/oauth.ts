import type { Express } from "express";

// OAuth stub – OAuth externo não está em uso.
// O sistema usa autenticação local via /api/auth/login
export function registerOAuthRoutes(_app: Express) {
  // sem-op
}
